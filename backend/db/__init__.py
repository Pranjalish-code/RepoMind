# db/__init__.py
"""
Database package for RepoMind AI.

Import directly from sub-modules to avoid circular imports:
  from db.session import get_db, create_all_tables
  from db.models import User, Repository, ...
  from db.crud import create_user, get_repository, ...
"""
